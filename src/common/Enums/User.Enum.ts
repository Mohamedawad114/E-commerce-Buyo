export enum Sys_Role {
    admin = 'admin',
    user = 'user',
    moderator = 'moderator',
}

export enum Provider {
    google = 'google',
    system = 'system',
    facbook = 'facebook',
}
export enum Gender{
    male = 'male',
    female ='female'
}